server:
  port: 8084

spring:
  application:
    name: mishes-pedido
  datasource:
    url: jdbc:mysql://localhost:3306/${param.dbname}?useSSL=false&serverTimezone=UTC
    username: root
    password: Mishes2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always

param:
  dbname: mishes_pedido_db
  productos:
    url: http://localhost:8083/
  clientes:
    url: http://localhost:8082/

jwt:
  secret: TuClaveSecretaSuperSeguraParaDuocUC2026!

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/