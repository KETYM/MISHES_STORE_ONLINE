server:
  port: 8083

spring:
  application:
    name: mishes-pago # Nombre para identificarlo en Eureka
  datasource:
    url: jdbc:mysql://localhost:3306/mishes_pago_db?createDatabaseIfNotExist=true&serverTimezone=UTC&useSSL=false
    username: root
    password: Mishes2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  flyway:
    url: jdbc:mysql://localhost:3306/mishes_pago_db?createDatabaseIfNotExist=true&serverTimezone=UTC&useSSL=false
    user: root
    password: Mishes2026
    baseline-on-migrate: true

param:
  clientes:
    url: localhost:8080
  pedidos:
    url: localhost:8080

jwt:
  secret: TuClaveSecretaSuperSeguraParaDuocUC2026!

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/