server:
  port: 8085

spring:
  application:
    name: ms-producto
  datasource:
    url: jdbc:mysql://localhost:3306/tienda_db?useSSL=false&serverTimezone=UTC
    username: root
    password: Mishes2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true

jwt:
  secret: TuClaveSecretaSuperSeguraParaDuocUC2026!

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/